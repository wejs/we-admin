{{notification-container position="top"}}

{{#if session.needsReload}}
  {{bootstrap-loading}}
{{else}}
  {{#if session.isAuthenticated}}
    {{partial 'partials/home-not-manager'}}
  {{else}}
    {{partial 'partials/home-un-authenticated'}}
  {{/if}}

{{/if}}

{{partial 'partials/file-selector-modal'}}