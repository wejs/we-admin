<h1 class="page-header">{{t 'user.find'}}</h1>

<div class="actions">
  {{#acl-btn-create model="user" url="/users/create"}}{{t 'Register'}}{{/acl-btn-create}}
</div>
<br>

{{conteudos-table
  data=model.users
  columns=model.columns
  pageSize=25
  useFilteringByColumns=false
  deleteRecord='deleteRecord'
}}